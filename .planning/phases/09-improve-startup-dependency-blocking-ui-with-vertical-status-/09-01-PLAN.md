---
phase: 09-improve-startup-dependency-blocking-ui-with-vertical-status-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/services/startupSecurity.ts
  - src/main/index.ts
  - src/preload/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dependency checks run progressively and expose structured results to renderer"
    - "Blocking UI shows detailed diagnostics after ALL checks complete"
    - "Retry mechanism re-runs full dependency check"
  artifacts:
    - path: "src/main/services/startupSecurity.ts"
      provides: "Extended verifyStartupSafety with progressive callbacks"
    - path: "src/main/index.ts"
      provides: "Updated blocking window with stepper UI instead of error text"
    - path: "src/preload/index.ts"
      provides: "New IPC for staged dependency check with progress"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/services/startupSecurity.ts"
      via: "verifyStartupSafety invocation"
    - from: "src/preload/index.ts"
      to: "src/main/index.ts"
      via: "IPC channel"
---

<objective>
Implement main-process changes to expose progressive dependency check results to renderer and update blocking UI with proper diagnostic display.

Purpose: Replace immediate blocking error with UI that shows each dependency check (SSH, Git, virtual-drive) with tick/cross, then transitions to full blocking only after ALL checks complete.
Output: Modified startup security service + updated blocking window IPC + preload contract.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-improve-startup-dependency-blocking-ui-with-vertical-status-/09-CONTEXT.md
@src/main/services/startupSecurity.ts
@src/main/index.ts
@src/renderer/src/features/splash-override/view/SplashViewOverride.tsx
@src/renderer/src/features/splash-override/viewmodel/useDhiSplashViewModel.ts

<constraints>
- D-01: Show dependency status UI BEFORE blocking
- D-02: Use pre-splash (unsafe startup) window as display surface
- D-07: Run ALL dependency checks FIRST before blocking
- D-11: On retry: re-run ALL dependency checks (full recheck)
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend startupSecurity to expose progressive dependency check results</name>
  <files>src/main/services/startupSecurity.ts</files>
  <behavior>
    - verifyStartupSafety returns diagnostic array with all dependency statuses.
    - Each diagnostic includes: dependency (ssh|git|virtual-drive), available (bool), command, message.
    - Retry triggers fresh full recheck.
  </behavior>
  <action>Extend verifyStartupSafety or create new function to return structured diagnostics array with tick/cross per dependency. Add callback support for progress updates.</action>
  <verify>
    <automated>npm run test -- src/main/services/startupSecurity.test.ts</automated>
  </verify>
  <done>Dependency check returns structured array: [{ dependency: 'ssh', available: true/false, command: '...', message: '...' }, ...]</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create IPC method for staged dependency check</name>
  <files>src/main/index.ts, src/preload/index.ts</files>
  <behavior>
    - Renderer can request staged dependency check with progress callbacks.
    - IPC channel exposes dependency status to renderer stepper component.
    - Retry endpoint re-runs full check.
  </behavior>
  <action>Add new IPC handler in main process for staged dependency check. Expose via preload bridge. Update showUnsafeStartupWindow to receive diagnostic array.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>New IPC method available: checkHostDependencies() → { diagnostics: [...] }</done>
</task>

<task type="auto">
  <name>Task 3: Update blocking window to show dependency stepper</name>
  <files>src/main/index.ts, src/preload/index.ts</files>
  <behavior>
    - Blocking window displays vertical stepper (reusing splash stepper styling).
    - Shows all 3 checks: SSH, Git, virtual-drive with icons (loading, check, cross).
    - After all complete: shows blocking if any failed with retry button.
  </behavior>
  <action>Modify showUnsafeStartupWindow to accept diagnostic array and render stepper component. Pass diagnostic data to renderer for stepper display.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Blocking window shows vertical stepper with tick/cross per dependency, then blocking state if any failed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host runtime -> dependency checks | Host PATH/config binaries may be missing/tampered |
| main process -> renderer | IPC data must be sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-09-01 | T | dependency check results | mitigate | Validate command output before exposing to renderer. |
| T-09-02 | I | IPC data exposure | mitigate | Sanitize diagnostic messages; don't expose raw command stderr. |
</threat_model>

<verification>
- `npm run test -- src/main/services/startupSecurity.test.ts`
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Dependency checks show progressive status (loading → tick/cross) in UI.
- After ALL checks complete, if any failed: show full blocking with diagnostics.
- Retry button re-runs full dependency check.
- UI reuses splash stepper styling for consistency.
</success_criteria>

<output>
After completion, create `09-01-SUMMARY.md`
</output>