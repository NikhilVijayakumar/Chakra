---
phase: 09-improve-startup-dependency-blocking-ui-with-vertical-status-
plan: 02
type: execute
wave: 2
depends_on: [09-01-PLAN.md]
files_modified:
  - src/renderer/src/features/splash-override/view/
  - src/renderer/src/features/splash-override/viewmodel/
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vertical stepper shows each dependency with loading → tick/cross states"
    - "Retry button triggers full recheck and returns to checking state first"
    - "Visual consistency with existing splash stepper"
  artifacts:
    - path: "src/renderer/src/features/splash-override/view/DependencyStepper.tsx"
      provides: "Vertical stepper component for dependency display"
    - path: "src/renderer/src/features/splash-override/viewmodel/useDependencyCheckViewModel.ts"
      provides: "State management for dependency check flow"
  key_links:
    - from: "useDependencyCheckViewModel.ts"
      to: "src/preload/index.ts"
      via: "checkHostDependencies IPC"
    - from: "DependencyStepper.tsx"
      to: "useDependencyCheckViewModel.ts"
      via: "React hook"
---

<objective>
Create renderer components for dependency stepper UI and integrate into blocking window flow.

Purpose: Render the vertical stepper showing each dependency check with loading → tick/cross states, with retry mechanism to re-run checks after user fixes dependencies.
Output: DependencyStepper component + viewmodel + integrated blocking UI.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-improve-startup-dependency-blocking-ui-with-vertical-status-/09-CONTEXT.md
@.planning/phases/09-improve-startup-dependency-blocking-ui-with-vertical-status-/09-01-PLAN.md
@src/renderer/src/features/splash-override/view/SplashViewOverride.tsx
@src/renderer/src/features/splash-override/viewmodel/useDhiSplashViewModel.ts
@src/preload/index.ts

<constraints>
- D-03: Vertical stepper for 3 dependencies: SSH, Git, virtual-drive
- D-04: Each shows loading → green checkmark OR red cross
- D-05: Show diagnostic message per failed dependency
- D-08: After all checks complete, if any failed → blocking UI
- D-10: Provide retry mechanism
- D-12: After successful retry (all pass) → proceed to normal startup
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DependencyStepper component</name>
  <files>src/renderer/src/features/splash-override/view/DependencyStepper.tsx</files>
  <behavior>
    - Vertical stepper shows 3 stages: SSH, Git, virtual-drive.
    - Each stage shows icon matching state: loading (spinner), success (checkmark), error (cross).
    - Displays diagnostic message for failed dependencies.
    - Reuses MUI icons from SplashViewOverride.
  </behavior>
  <action>Create DependencyStepper component following SplashViewOverride styling pattern. Use CheckCircleIcon, ErrorOutlineIcon, CircularProgress for states.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>DependencyStepper renders vertical list with icons per dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useDependencyCheckViewModel</name>
  <files>src/renderer/src/features/splash-override/viewmodel/useDependencyCheckViewModel.ts</files>
  <behavior>
    - Manages dependency check states: idle, checking, completed, blocking.
    - Calls IPC to run checks with progress/update callbacks.
    - On blocking: shows retry button.
    - On retry: returns to checking state, runs full recheck.
    - On success: transitions to startup.
  </behavior>
  <action>Create viewmodel hook for dependency check flow. Follow pattern from useDhiSplashViewModel but simplified for dependency checking.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Viewmodel manages: idle → checking → (completed success | blocking with retry)</done>
</task>

<task type="auto">
  <name>Task 3: Integrate stepper into blocking window</name>
  <files>src/main/index.ts, src/renderer/src/features/splash-override/view/</files>
  <behavior>
    - Blocking window now renders DependencyStepper component.
    - Shows checking state: stepper with loading icons running.
    - After all complete: if any failed, shows blocking state with retry.
    - Retry button triggers full recheck loop.
    - Success: proceed to startup.
  </behavior>
  <action>Update blocking window rendering to include stepper component. Wire viewmodel to IPC for dependency checks. Add retry handler.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Blocking window shows vertical stepper → (blocking state + retry) OR (proceed to startup)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main | IPC for dependency check results |
| user -> retry | User-triggered external dependency validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-09-03 | I | retry button | mitigate | Always re-run full check, don't trust cached results. |
| T-09-04 | D | state transitions | mitigate | Validate results before state changes. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
- Manual: Launch app without SSH/Git/virtual-drive to see stepper and retry flow
</verification>

<success_criteria>
- Vertical stepper displays SSH, Git, virtual-drive with loading → tick/cross icons.
- Blocking state shows failed dependencies with diagnostic messages.
- Retry button re-runs full dependency check.
- Successful check allows startup to proceed.
</success_criteria>

<output>
After completion, create `09-02-SUMMARY.md`
</output>