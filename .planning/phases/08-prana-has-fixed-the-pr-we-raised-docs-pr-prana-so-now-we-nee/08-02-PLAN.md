---
phase: 08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/src/features/splash-override/viewmodel/useDhiSplashViewModel.ts
  - src/renderer/src/features/splash-override/view/SplashContainerOverride.test.tsx
  - src/renderer/src/main.test.tsx
autonomous: true
requirements: [P8-IPC-01, P8-REG-01]
must_haves:
  truths:
    - "Renderer can consume shared host dependency capability status through preload contracts"
    - "Splash consumes shared capability results without owning dependency-check implementation"
    - "Existing login and /apps route contract remains intact after integration"
  artifacts:
    - path: "src/preload/index.ts"
      provides: "Capability check IPC exposure for renderer consumers"
    - path: "src/preload/index.d.ts"
      provides: "Typed dependency capability contract for window.api"
    - path: "src/renderer/src/features/splash-override/viewmodel/useDhiSplashViewModel.ts"
      provides: "Splash consumption of shared capability results"
  key_links:
    - from: "src/preload/index.ts"
      to: "renderer splash view-model"
      via: "window.api capability API"
      pattern: "api\.app|api\.auth|dependency"
    - from: "splash view-model"
      to: "startup dependency contract"
      via: "consume status, do not implement detection"
      pattern: "getStatus|capability|diagnostics"
---

<objective>
Expose reusable dependency capability APIs through Chakra preload contracts and make splash consume those results without architecture coupling.

Purpose: Preserve page-agnostic capability architecture while ensuring splash and route contracts remain stable.
Output: Preload contract updates + splash consumer refactor + regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee/08-CONTEXT.md
@.planning/phases/08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee/08-RESEARCH.md
@.planning/phases/08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee/08-01-SUMMARY.md
@src/preload/index.ts
@src/preload/index.d.ts
@src/renderer/src/features/splash-override/viewmodel/useDhiSplashViewModel.ts
@src/renderer/src/features/splash-override/view/SplashContainerOverride.test.tsx
@src/renderer/src/main.test.tsx

<interfaces>
From `src/preload/index.ts`/`src/preload/index.d.ts`:
- Existing `window.api.app` and `window.api.auth` contracts are the integration boundary.

From upstream capability contract:
- dependency result shape: { passed, missing, diagnostics[] }
- caller owns UI decision; service owns capability evaluation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preload API contract for shared dependency capability status</name>
  <files>src/preload/index.ts, src/preload/index.d.ts</files>
  <behavior>
    - Renderer receives typed dependency capability results via preload.
    - API shape preserves dependency-specific diagnostics.
    - Contract remains generic and usable by splash/settings/diagnostics pages.
  </behavior>
  <action>Expose dependency capability read/invoke methods in preload runtime and declaration contracts (per D-02, D-03, D-04, D-05). Keep API page-agnostic and avoid splash-specific method naming.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Typed preload dependency capability API is available and compiles with existing renderer contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor splash view-model to consume shared dependency results</name>
  <files>src/renderer/src/features/splash-override/viewmodel/useDhiSplashViewModel.ts, src/renderer/src/features/splash-override/view/SplashContainerOverride.test.tsx, src/renderer/src/main.test.tsx</files>
  <behavior>
    - Splash consumes capability status and renders/acts accordingly.
    - Splash no longer owns dependency binary detection logic.
    - Existing no-session/session routing semantics remain unchanged.
  </behavior>
  <action>Replace any splash-owned dependency probing with consumption of shared preload capability API (per D-05, D-11, D-12). Preserve current auth-first route handoff behavior to `/login` and `/apps`.</action>
  <verify>
    <automated>npm run test -- src/renderer/src/features/splash-override/view/SplashContainerOverride.test.tsx src/renderer/src/main.test.tsx</automated>
  </verify>
  <done>Splash remains a consumer of shared capability status and route contract tests remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Run end-to-end regression checks for phase handoff</name>
  <files>src/preload/index.ts, src/preload/index.d.ts, src/renderer/src/features/splash-override/viewmodel/useDhiSplashViewModel.ts</files>
  <action>Execute final verification for preload + splash integration and fix any regressions before phase verification.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build/type/test checks pass and phase-8 dependency architecture remains decoupled from splash implementation details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main process -> preload bridge | Privileged dependency diagnostics cross into renderer-accessible API |
| preload -> renderer splash | Untrusted UI consumes capability diagnostics and drives route flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | S | preload API consumers | mitigate | Keep capability contract read-only and avoid exposing arbitrary command execution through preload. |
| T-08-06 | I | renderer diagnostics rendering | mitigate | Restrict displayed diagnostics to operational messages and avoid secret-bearing payloads. |
| T-08-07 | T | splash dependency flow | mitigate | Ensure splash only consumes authoritative main-process capability results, not local binary probing logic. |
| T-08-08 | D | route regression | mitigate | Maintain route tests for `/login` and `/apps` handoff to prevent startup lockout or misrouting. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run test -- src/renderer/src/features/splash-override/view/SplashContainerOverride.test.tsx src/renderer/src/main.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Preload exposes a reusable, typed dependency capability contract.
- Splash consumes shared capability results and does not own dependency detection logic.
- Existing auth/splash/apps route behavior remains intact under regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee/08-02-SUMMARY.md`
</output>
