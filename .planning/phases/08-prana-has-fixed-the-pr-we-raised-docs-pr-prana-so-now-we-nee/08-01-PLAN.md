---
phase: 08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/services/startupSecurity.ts
  - src/main/index.ts
  - src/main/services/startupSecurity.test.ts
autonomous: true
requirements: [P8-DEP-01, P8-DEP-02, P8-DRV-01, P8-DRV-02]
must_haves:
  truths:
    - "Startup blocks when SSH, Git, or virtual-drive runtime dependency is missing"
    - "Dependency capability checks remain reusable and not splash-owned"
    - "Drive mount/eject lifecycle is enforced by app start/stop orchestration"
  artifacts:
    - path: "src/main/services/startupSecurity.ts"
      provides: "Shared startup dependency gate consuming reusable capability results"
    - path: "src/main/index.ts"
      provides: "App lifecycle hooks for startup mount and shutdown eject"
    - path: "src/main/services/startupSecurity.test.ts"
      provides: "Regression tests for dependency gate and fail-closed behavior"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/services/startupSecurity.ts"
      via: "startup safety invocation"
      pattern: "verifyStartupSafety"
    - from: "src/main/index.ts"
      to: "drive lifecycle orchestration"
      via: "app start and stop handlers"
      pattern: "virtualDrives|before-quit|will-quit"
---

<objective>
Implement main-process dependency capability enforcement and drive lifecycle orchestration using upstream Prana contracts, while preserving Chakra fail-closed startup semantics.

Purpose: Ensure host dependencies and drive policy/lifecycle are enforced at runtime core boundaries, not UI-specific layers.
Output: Startup dependency gate + lifecycle wiring + regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee/08-CONTEXT.md
@.planning/phases/08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee/08-RESEARCH.md
@src/main/services/startupSecurity.ts
@src/main/index.ts
@src/main/services/startupSecurity.test.ts

<interfaces>
From E:/Python/prana/src/main/services/hostDependencyCapabilityService.ts:
- evaluate(): Promise<{ passed: boolean; missing: ('ssh'|'git'|'virtual-drive')[]; diagnostics: ... }>

From E:/Python/prana/src/main/services/driveControllerService.ts:
- getPolicy(): { clientManaged: boolean }

From E:/Python/prana/src/main/services/startupOrchestratorService.ts:
- startup stage id: host-dependencies
- dependency checks are blocking before governance stage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend startup safety to include reusable host dependency capability checks</name>
  <files>src/main/services/startupSecurity.ts, src/main/services/startupSecurity.test.ts</files>
  <behavior>
    - Test missing ssh/git/virtual-drive dependency paths block startup with dependency-specific diagnostics.
    - Test successful capability evaluation preserves current allowed startup behavior.
    - Test behavior stays deterministic across repeated checks.
  </behavior>
  <action>Integrate a reusable dependency capability evaluation step into startup safety flow (per D-01, D-02, D-03, D-06). Do not implement splash-specific probing in this service. Keep fail-closed behavior and preserve existing SSH/config validation semantics.</action>
  <verify>
    <automated>npm run test -- src/main/services/startupSecurity.test.ts</automated>
  </verify>
  <done>Startup safety returns structured blocking results for missing host dependencies and remains backward-compatible for valid environments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire app-level drive lifecycle mount/eject with client-owned policy guardrails</name>
  <files>src/main/index.ts, src/main/services/startupSecurity.test.ts</files>
  <behavior>
    - App startup triggers required mount/open behavior without splash-owned coupling.
    - App shutdown triggers eject/unmount behavior reliably.
    - Client-managed policy remains host-owned; runtime mechanics remain in Prana.
  </behavior>
  <action>Update main-process bootstrap/shutdown orchestration to enforce mount-on-start and eject-on-stop semantics (per D-07, D-08, D-09, D-10). Preserve existing startup sequencing and do not regress phase-7 auth-first flow (D-12).</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Main process includes explicit lifecycle hooks for drive startup/shutdown and no startup regression is introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Validate full startup contract after dependency and lifecycle wiring</name>
  <files>src/main/index.ts, src/main/services/startupSecurity.ts, src/main/services/startupSecurity.test.ts</files>
  <action>Run phase-level verification for main-process changes and fix any regressions before handing off to renderer/preload integration.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Typecheck/build pass and startup dependency/lifecycle contract changes are stable for downstream plan 08-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| startup-config -> main bootstrap | Untrusted env/config values affect startup decisions |
| host runtime -> dependency capability checks | Host PATH/config binaries may be missing/tampered |
| main lifecycle -> virtual drive provider | Mount/unmount operations cross process/OS boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `startupSecurity` dependency checks | mitigate | Validate dependency command results and emit structured diagnostics per dependency before startup progression. |
| T-08-02 | D | drive lifecycle orchestration | mitigate | Enforce deterministic mount-on-start and eject-on-stop hooks with fail-closed handling when lifecycle actions fail. |
| T-08-03 | I | startup error reporting | mitigate | Restrict error payloads to operational diagnostics; avoid exposing sensitive secrets in surfaced messages. |
| T-08-04 | R | startup transitions | mitigate | Preserve explicit staged startup checks and maintain deterministic logs for failure attribution. |
</threat_model>

<verification>
- `npm run test -- src/main/services/startupSecurity.test.ts`
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Missing SSH/Git/virtual-drive runtime dependencies block startup with explicit diagnostics.
- Dependency capability evaluation is reusable and not implemented as splash-owned logic.
- Drive lifecycle behavior mounts at startup and ejects at shutdown with no phase-7 route/auth regression.
</success_criteria>

<output>
After completion, create `.planning/phases/08-prana-has-fixed-the-pr-we-raised-docs-pr-prana-so-now-we-nee/08-01-SUMMARY.md`
</output>
