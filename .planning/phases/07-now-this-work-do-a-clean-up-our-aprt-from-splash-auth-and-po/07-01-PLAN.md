---
phase: 07-now-this-work-do-a-clean-up-our-aprt-from-splash-auth-and-po
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - vitest.config.ts
  - tsconfig.node.json
  - tsconfig.web.json
  - src/main/index.ts
  - src/main/services/runtimeEnv.ts
  - src/main/services/startupSecurity.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/main/services/dharmaIpcHandlers.ts
  - src/main/services/dharmaSyncService.ts
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06
must_haves:
  truths:
    - "App startup uses Chakra naming in touched startup/auth surfaces and no longer emits DHI-branded startup labels in updated paths"
    - "Dharma dependency and preload/main runtime references are removed without breaking build/typecheck"
    - "Splash/auth startup behavior remains fail-closed with SSH gate and env-to-cache flow intact"
  artifacts:
    - path: package.json
      provides: "No dharma dependency and Chakra package metadata alignment"
      min_lines: 40
    - path: src/main/index.ts
      provides: "Chakra-branded startup path with required Prana initialization only"
      min_lines: 120
    - path: src/preload/index.ts
      provides: "Renderer API contract without dharma surface"
      min_lines: 120
    - path: src/main/services/runtimeEnv.ts
      provides: "Cache/env path normalization for Chakra runtime behavior"
      min_lines: 80
    - path: src/main/services/startupSecurity.ts
      provides: "Startup safety contract aligned with Chakra cleanup"
      min_lines: 60
  key_links:
    - from: src/main/index.ts
      to: src/main/services/runtimeEnv.ts
      via: "startup bootstrap + env/cache bridge"
      pattern: "loadWorkspaceEnvFile|bridgeMainViteDhiEnvToRuntime|ensureWritableDevRuntimePaths"
    - from: src/preload/index.ts
      to: src/preload/index.d.ts
      via: "runtime API surface parity"
      pattern: "contextBridge\.exposeInMainWorld\('api'"
    - from: package.json
      to: electron.vite.config.ts
      via: "dependency/alias consistency after Dharma removal"
      pattern: "dharma"
---

<objective>
Perform core startup cleanup for Chakra: remove Dharma integration, normalize DHI to Chakra naming in touched startup/auth surfaces, and preserve required Prana initialization with splash SSH and env-cache behavior.

Purpose: This creates a stable foundation for route-level post-login cleanup by removing obsolete platform wiring and preventing mixed runtime identities.
Output: Buildable runtime surface without Dharma and with startup/env/cache behavior still operational.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-now-this-work-do-a-clean-up-our-aprt-from-splash-auth-and-po/07-CONTEXT.md
@.planning/phases/07-now-this-work-do-a-clean-up-our-aprt-from-splash-auth-and-po/07-RESEARCH.md
@.planning/phases/07-now-this-work-do-a-clean-up-our-aprt-from-splash-auth-and-po/07-VALIDATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> preload API | Renderer can only call APIs exposed by preload; removing Dharma must not leave unsafe or dangling channels |
| startup env -> runtime bootstrap | Untrusted/misconfigured env values can alter startup flow and cache behavior |
| main process boot -> splash/auth gate | Startup must fail closed when safety checks fail and must not bypass SSH gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | E | `src/preload/index.ts`, `src/preload/index.d.ts` | mitigate | Remove Dharma API surface and keep preload contracts strictly aligned to active channels only; typecheck to ensure no stale privileged calls remain |
| T-07-02 | T | `src/main/services/runtimeEnv.ts` | mitigate | Preserve deterministic env normalization and cache path initialization while renaming touched DHI labels; keep fail-safe defaults and guard missing values |
| T-07-03 | D | `src/main/index.ts`, `src/main/services/startupSecurity.ts` | mitigate | Keep startup safety validation and fail-closed behavior active while pruning non-required startup branches; verify boot still builds and starts |
| T-07-04 | I | `src/main/index.ts` logs and startup UI labels | mitigate | Replace DHI branding strings in touched startup/auth surfaces to avoid identity confusion and misleading operational logs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remove Dharma dependency and runtime integration surfaces</name>
  <files>package.json, electron.vite.config.ts, vitest.config.ts, tsconfig.node.json, tsconfig.web.json, src/preload/index.ts, src/preload/index.d.ts, src/main/index.ts, src/main/services/dharmaIpcHandlers.ts, src/main/services/dharmaSyncService.ts</files>
  <action>
    Hard-remove Dharma from Chakra runtime: remove `dharma` dependency from package.json, remove Dharma aliases from build/test/type configs, remove Dharma API contract from preload files, and delete/retire Dharma main-process handler wiring from startup bootstrap. Remove now-unused Dharma service files or detach them from build path so no runtime or type references remain.
  </action>
  <verify>
    npm run typecheck
  </verify>
  <done>
    Dharma symbols are absent from package/runtime preload/main startup wiring and TypeScript compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Normalize startup naming and preserve required startup safety/cache behavior</name>
  <files>src/main/index.ts, src/main/services/runtimeEnv.ts, src/main/services/startupSecurity.ts</files>
  <action>
    Update touched DHI-facing startup/auth naming and labels to Chakra while preserving required Prana initialization. Keep splash-related SSH safety gating and env-to-cache behavior intact. Ensure startup sequence remains explicit and fail-closed, with cache/runtime path semantics aligned to Chakra app naming where touched in this phase.
  </action>
  <verify>
    npm run build
  </verify>
  <done>
    Startup/auth touched surfaces use Chakra naming and still preserve required Prana bootstrap + safety/cache behavior.
  </done>
</task>

</tasks>

<verification>
- Typecheck passes with Dharma removed from active runtime contracts.
- Build passes after startup cleanup and naming changes.
- Startup safety and env/cache behavior remain active in updated bootstrap path.
</verification>

<success_criteria>
- CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05, CLEAN-06 are fully covered by plan outputs.
- No Dharma dependency or runtime API surface remains in active app flow.
- Chakra naming is applied in touched startup/auth surfaces without breaking boot.
</success_criteria>

<output>
After completion, create the matching SUMMARY.md for this plan in the same phase directory.
</output>
